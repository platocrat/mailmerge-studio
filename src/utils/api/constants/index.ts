export const JWT_ERROR_MESSAGES = {
  expiredJWT: `jwt expired`
}

export const MAX_AGE = {
  ACCESS_TOKEN: 60 * 60 * 1 / 2, // 30 minutes
  SESSION: 60 * 60 * 2 // 2 hours
}

export const COOKIE_NAMES = {
  USER_AUTH: 'mailmerge-studio-user-auth',
}